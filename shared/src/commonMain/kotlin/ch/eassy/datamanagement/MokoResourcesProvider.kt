package ch.eassy.datamanagement

class MokoResourcesProvider () {
    // we'll use this later for more complex resources
    //fun getConfiguration() = MokoResources.files.config
    //fun getString() = StringDesc.Resource(MokoResources.strings.my_string).toString()
}
